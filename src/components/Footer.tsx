const Footer = () => (
  <footer className="border-t border-border py-6 text-center">
    <p className="font-mono text-[10px] text-muted-foreground">
      built by manu · {new Date().getFullYear()}
    </p>
  </footer>
);

export default Footer;
